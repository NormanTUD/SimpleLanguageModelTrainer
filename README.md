# SimpleLanguageModelTrainer

Requirements:

```console
python3 -m venv .environment
pip3 install torch
pip3 install transformers
```

Example call:

```console
python3 train.py --learning-rate 0.01 --num-epochs 0 --test-string "hallo welt, " --filename README.md
```
